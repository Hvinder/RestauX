const port = "3000";
const baseurl = `http://localhost:${port}`;

export const API_URLS = {
  places_endpoint: `${baseurl}/places`,
  placeDetails_endpoint: `${baseurl}/placeDetails`,
  photo_endpoint: `${baseurl}/photo`,
};
